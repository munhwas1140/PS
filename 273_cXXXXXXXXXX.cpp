#include <bits/stdc++.h>
using namespace std;
#define fastio cin.tie(0)->sync_with_stdio(0)
using ll = long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;

int main() {
    fastio;
    int n; cin >> n;
    set<int> s;
    for(int i = 0; i < n; i++) {
        int tmp; cin >> tmp;
        s.insert(tmp);
    }

    for(int i = 0; i < n; i++) {

    }


    return 0;
}