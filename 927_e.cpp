#include <bits/stdc++.h>
using namespace std;
#define fastio cin.tie(0)->sync_with_stdio(0)
using ll = long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
int x[5000], y[5000];
int main() {
    fastio;
    int n, a, l;
    cin >> n >> a >> l;
    for(int i = 0; i < n; i++) {
        cin >> x[i] >> y[i];
    }

    

    return 0;
}