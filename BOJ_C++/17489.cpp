#include <bits/stdc++.h>
using namespace std;
#define fastio cin.tie(0)->sync_with_stdio(0)
using ll = long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
int dx[] = {1,-1,0,0};
int dy[] = {0,0,1,-1};
int main() {
    fastio;
    int n, m, l;
    cin >> n >> m >> l;
    string str; cin >> str;

    vector<string> a(n);
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }

    

    

    return 0;
}